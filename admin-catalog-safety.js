(function(){'use strict';
const $=id=>document.getElementById(id);
function ensureGuidance(){const form=$('productForm');if(!form||$('catalogSafetyNote'))return;const note=document.createElement('div');note.id='catalogSafetyNote';note.setAttribute('data-csp-style','csp-cssText-admin-catalog-safety-1');note.innerHTML='<b>Safe publish workflow:</b> Save new products as Inactive → add at least one SKU in Variants & Inventory → set available stock above 0 → return here and activate. Production blocks activation until inventory is ready.';form.parentNode.insertBefore(note,form)}
function alignStatusControl(){const status=$('isActive'),id=$('productId'),modal=$('productModal');if(!status||!id||!modal)return;const active=status.querySelector('option[value="true"]'),inactive=status.querySelector('option[value="false"]');if(active&&active.textContent!=='Active — requires stocked SKU')active.textContent='Active — requires stocked SKU';if(inactive&&inactive.textContent!=='Inactive — safe while setting up')inactive.textContent='Inactive — safe while setting up';if(modal.classList.contains('open')&&!String(id.value||'').trim()&&status.value!=='false')status.value='false'}
function addProductManagementHint(){const card=[...document.querySelectorAll('.card')].find(x=>x.querySelector('.card-header h2')?.textContent.trim()==='Product Management');if(!card||$('catalogPublishHint'))return;const hint=document.createElement('div');hint.id='catalogPublishHint';hint.setAttribute('data-csp-style','csp-cssText-admin-catalog-safety-2');hint.innerHTML='<b>Launch guard:</b> New products stay inactive until a SKU has positive available stock. This prevents an untracked product from becoming sellable.';card.querySelector('.card-body')?.prepend(hint)}
function sync(){ensureGuidance();alignStatusControl();addProductManagementHint()}
let scheduled=false;function scheduleSync(){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;sync()})}
const observer=new MutationObserver(scheduleSync);observer.observe(document.documentElement,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',sync,{once:true});else sync();
})();
