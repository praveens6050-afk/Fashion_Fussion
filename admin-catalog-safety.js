(function(){'use strict';
const $=id=>document.getElementById(id);
function ensureGuidance(){const form=$('productForm');if(!form||$('catalogSafetyNote'))return;const note=document.createElement('div');note.id='catalogSafetyNote';note.style.cssText='margin:0 20px 4px;padding:11px 13px;border:1px solid #d7e3ff;background:#f6f9ff;border-radius:6px;font-size:12px;color:#35507a;line-height:1.45';note.innerHTML='<b>Safe publish workflow:</b> Save new products as Inactive → add at least one SKU in Variants & Inventory → set available stock above 0 → return here and activate. Production blocks activation until inventory is ready.';form.parentNode.insertBefore(note,form)}
function alignStatusControl(){const status=$('isActive'),id=$('productId'),modal=$('productModal');if(!status||!id||!modal)return;const active=status.querySelector('option[value="true"]'),inactive=status.querySelector('option[value="false"]');if(active)active.textContent='Active — requires stocked SKU';if(inactive)inactive.textContent='Inactive — safe while setting up';if(modal.classList.contains('open')&&!String(id.value||'').trim())status.value='false'}
function addProductManagementHint(){const card=[...document.querySelectorAll('.card')].find(x=>x.querySelector('.card-header h2')?.textContent.trim()==='Product Management');if(!card||$('catalogPublishHint'))return;const hint=document.createElement('div');hint.id='catalogPublishHint';hint.style.cssText='margin:12px 18px 0;padding:10px 12px;background:#fff8e6;border:1px solid #f2d58a;border-radius:6px;font-size:12px;color:#725610';hint.innerHTML='<b>Launch guard:</b> New products stay inactive until a SKU has positive available stock. This prevents an untracked product from becoming sellable.';card.querySelector('.card-body')?.prepend(hint)}
function sync(){ensureGuidance();alignStatusControl();addProductManagementHint()}
const observer=new MutationObserver(sync);observer.observe(document.documentElement,{subtree:true,childList:true,attributes:true,attributeFilter:['class','value']});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',sync,{once:true});else sync();
})();
