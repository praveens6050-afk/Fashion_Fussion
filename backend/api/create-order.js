const {
  KEY_ID, KEY_SECRET, SUPABASE_URL, serverHeaders, cors, json, readBody, basicAuth,
  getSupabaseUser, calculate, normalizePaymentMethod, paymentPricing
} = require('../lib');
const { getDiscounts } = require('./quote-order');
async function rest(path,options={}){const response=await fetch(SUPABASE_URL+'/rest/v1/'+path,{...options,headers:{...serverHeaders,...(options.headers||{})}});const data=await response.json().catch(()=>null);if(!response.ok){const error=new Error(data?.message||data?.error||'Store database request failed');error.status=response.status;error.data=data;throw error}return data}
async function rpc(name,args){const response=await fetch(SUPABASE_URL+'/rest/v1/rpc/'+name,{method:'POST',headers:{...serverHeaders,'Content-Type':'application/json'},body:JSON.stringify(args)});const data=await response.json().catch(()=>null);if(!response.ok)throw new Error(data?.message||data?.error||'Checkout operation failed');return data}
async function getAddress(userId,requestedId){const filter=requestedId&&/^\d+$/.test(String(requestedId))?'id=eq.'+encodeURIComponent(requestedId):'is_default=eq.true';const data=await rest('customer_addresses?user_id=eq.'+encodeURIComponent(userId)+'&'+filter+'&select=id,label,full_name,phone,address_line1,address_line2,city,state,postal_code,country&limit=1');return data?.[0]||null}
async function getBusinessProfile(userId){const data=await rest('business_profiles?user_id=eq.'+encodeURIComponent(userId)+'&select=user_id,business_name,gstin,billing_address&limit=1');return data?.[0]||null}
function cleanAddress(address){if(!address)throw new Error('Please add a delivery address before checkout.');const required=[address.full_name,address.phone,address.address_line1,address.city,address.state,address.postal_code,address.country];if(required.some(value=>!String(value||'').trim()))throw new Error('Please complete your delivery address before checkout.');if(!/^\d{6}$/.test(String(address.postal_code).trim()))throw new Error('Please use a valid 6-digit delivery PIN code.');return{id:address.id||null,label:String(address.label||'Home').trim(),full_name:String(address.full_name).trim(),phone:String(address.phone).trim(),address_line1:String(address.address_line1).trim(),address_line2:address.address_line2?String(address.address_line2).trim():null,city:String(address.city).trim(),state:String(address.state).trim(),postal_code:String(address.postal_code).trim(),country:String(address.country||'India').trim()}}
function businessSnapshot(profile,purchaseOrderNo){if(!profile?.business_name||!String(profile.business_name).trim())throw new Error('Save your business details before requesting a GST/business invoice.');const gstin=profile.gstin?String(profile.gstin).trim().toUpperCase():null;if(gstin&&!/^[0-9]{2}[A-Z]{5}[0-9]{4}[A-Z][1-9A-Z]Z[0-9A-Z]$/.test(gstin))throw new Error('Your saved GSTIN format is invalid. Please update Business & Bulk in My Account.');const po=String(purchaseOrderNo||'').trim();if(po.length>100)throw new Error('Purchase order number is too long.');return{is_business_order:true,business_name:String(profile.business_name).trim(),business_gstin:gstin,business_billing_address:profile.billing_address||null,purchase_order_no:po||null}}
function validateCheckoutKey(value){const key=String(value||'').trim();if(!/^[A-Za-z0-9_-]{16,100}$/.test(key))throw new Error('Checkout session is invalid. Please refresh checkout and try again.');return key}
async function findExisting(userId,checkoutKey){const rows=await rest('orders?user_id=eq.'+encodeURIComponent(userId)+'&checkout_key=eq.'+encodeURIComponent(checkoutKey)+'&select=id,display_order_id,razorpay_order_id,total_amount,currency,status,payment_method,payment_handling_fee,prepaid_discount,cod_fee_non_refundable&limit=1');return rows?.[0]||null}
async function enforceRateLimit(userId){const since=new Date(Date.now()-10*60*1000).toISOString();const response=await fetch(SUPABASE_URL+'/rest/v1/orders?user_id=eq.'+encodeURIComponent(userId)+'&created_at=gte.'+encodeURIComponent(since)+'&status=in.(creating,created,cod_pending)&select=id',{method:'HEAD',headers:{...serverHeaders,Prefer:'count=exact'}});if(!response.ok)throw new Error('Could not validate checkout rate limit');const count=Number((response.headers.get('content-range')||'0/0').split('/')[1]||0);if(count>=5)throw new Error('Too many checkout attempts. Please wait a few minutes before trying again.')}
function pricingPayload(calc,promo,payment){return{...calc,coupon_discount:promo.couponDiscount,gift_card_discount:promo.giftDiscount,payment_handling_fee:payment.payment_handling_fee,prepaid_discount:payment.prepaid_discount,cod_fee:payment.cod_fee,cod_fee_non_refundable:payment.cod_fee_non_refundable,payment_method:payment.payment_method,total:payment.total}}
function existingResponse(existing){return{idempotent:true,store_order_id:existing.id,display_order_id:existing.display_order_id,payment_method:existing.payment_method,status:existing.status,total:Number(existing.total_amount),completed:['paid','cod_pending','cod_collected'].includes(existing.status),order:existing.razorpay_order_id?{id:existing.razorpay_order_id,amount:Math.round(Number(existing.total_amount)*100),currency:existing.currency||'INR'}:null,key_id:existing.razorpay_order_id?KEY_ID:null}}
async function createRazorpayOrder(storeOrder,userId){if(!KEY_ID||!KEY_SECRET)throw new Error('Razorpay server keys are not configured');const response=await fetch('https://api.razorpay.com/v1/orders',{method:'POST',headers:{Authorization:basicAuth(),'Content-Type':'application/json'},body:JSON.stringify({amount:Math.round(Number(storeOrder.total_amount)*100),currency:storeOrder.currency||'INR',receipt:'FF_'+storeOrder.id,notes:{store:'Fashion_Fussion',user_id:userId,store_order_id:String(storeOrder.id)},partial_payment:false})});const order=await response.json().catch(()=>({}));if(!response.ok||!order?.id){const error=new Error(order?.error?.description||'Razorpay order creation failed');error.isRazorpay=true;throw error}return order}
async function reserveCheckout(orderId,userId){try{await rpc('reserve_order_promotions',{p_order_id:orderId,p_user_id:userId});await rpc('reserve_order_inventory',{p_order_id:orderId})}catch(error){await rpc('release_order_inventory',{p_order_id:orderId}).catch(()=>null);await rpc('release_order_promotions',{p_order_id:orderId,p_user_id:userId}).catch(()=>null);throw error}}
async function failCheckout(orderId,userId){await rpc('release_order_inventory',{p_order_id:orderId}).catch(()=>null);await rpc('release_order_promotions',{p_order_id:orderId,p_user_id:userId}).catch(()=>null);await rest('orders?id=eq.'+encodeURIComponent(orderId),{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:'payment_failed'})}).catch(()=>null)}
async function resumeExisting(existing,userId){if(['paid','cod_pending','cod_collected'].includes(existing.status))return existingResponse(existing);if(['payment_failed','expired','cancelled','cod_cancelled','refunded'].includes(existing.status)){const error=new Error('This checkout attempt is no longer active. Please try again.');error.retryWithNewCheckout=true;throw error}if(existing.status==='created'&&existing.razorpay_order_id)return existingResponse(existing);if(!['creating','created'].includes(existing.status))throw new Error('This checkout attempt cannot be resumed.');await reserveCheckout(existing.id,userId);if(existing.payment_method==='cod'){await rpc('finalize_checkout_order',{p_order_id:existing.id,p_user_id:userId,p_payment_id:null,p_payment_signature:null,p_target_status:'cod_pending',p_source:'cod_resume'});await rpc('commit_order_inventory',{p_order_id:existing.id});return{...existingResponse({...existing,status:'cod_pending'}),completed:true}}if(Number(existing.total_amount)===0){await rpc('finalize_checkout_order',{p_order_id:existing.id,p_user_id:userId,p_payment_id:null,p_payment_signature:null,p_target_status:'paid',p_source:'gift_card_resume'});await rpc('commit_order_inventory',{p_order_id:existing.id});return{...existingResponse({...existing,status:'paid'}),completed:true,zero_value:true}}const razorpayOrder=await createRazorpayOrder(existing,userId);await rest('orders?id=eq.'+encodeURIComponent(existing.id)+'&user_id=eq.'+encodeURIComponent(userId)+'&status=in.(creating,created)',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({razorpay_order_id:razorpayOrder.id,status:'created'})});return{...existingResponse({...existing,razorpay_order_id:razorpayOrder.id,status:'created'}),key_id:KEY_ID,order:{id:razorpayOrder.id,amount:razorpayOrder.amount,currency:razorpayOrder.currency}}}
module.exports=async function createOrder(req,res){cors(req,res);if(req.method==='OPTIONS'){res.statusCode=204;return res.end()}if(req.method!=='POST')return json(req,res,405,{error:'Method not allowed'});try{const user=await getSupabaseUser(req),body=await readBody(req),checkoutKey=validateCheckoutKey(body.checkout_key),paymentMethod=normalizePaymentMethod(body.payment_method),existing=await findExisting(user.id,checkoutKey);if(existing){if(existing.payment_method!==paymentMethod)return json(req,res,409,{error:'Payment method changed. Please start a new checkout attempt.',retry_with_new_checkout:true});try{return json(req,res,200,await resumeExisting(existing,user.id))}catch(error){return json(req,res,error.retryWithNewCheckout?409:400,{error:error.message||'Checkout could not be resumed',retry_with_new_checkout:Boolean(error.retryWithNewCheckout)})}}await enforceRateLimit(user.id);const calc=await calculate(body.items),promo=await getDiscounts(body,calc),payment=paymentPricing(promo.payable,paymentMethod);if(paymentMethod==='prepaid'&&payment.total>0&&(!KEY_ID||!KEY_SECRET))return json(req,res,500,{error:'Razorpay server keys are not configured'});const customerName=String(body.customer_name||'').trim();if(!customerName)throw new Error('Customer name is required');const address=cleanAddress(await getAddress(user.id,body.shipping_address?.id)),customerPhone=String(body.customer_phone||address.phone||'').trim();if(!customerPhone)throw new Error('Customer mobile number is required');let business={is_business_order:false,business_name:null,business_gstin:null,business_billing_address:null,purchase_order_no:null};if(body.business_invoice===true)business=businessSnapshot(await getBusinessProfile(user.id),body.purchase_order_no);const orderInsert={user_id:user.id,razorpay_order_id:null,total_amount:payment.total,currency:'INR',status:'creating',payment_method:paymentMethod,payment_handling_fee:payment.payment_handling_fee,prepaid_discount:payment.prepaid_discount,cod_fee_non_refundable:payment.cod_fee_non_refundable,checkout_key:checkoutKey,customer_name:customerName,customer_email:user.email||null,customer_phone:customerPhone,items:calc.items,coupon_code:promo.coupon?.code||null,coupon_discount:promo.couponDiscount,gift_card_code:promo.gift?.code||null,gift_card_discount:promo.giftDiscount,shipping_address:address,...business};let saved;try{const rows=await rest('orders',{method:'POST',headers:{'Content-Type':'application/json',Prefer:'return=representation'},body:JSON.stringify(orderInsert)});saved=rows?.[0]}catch(error){const raced=await findExisting(user.id,checkoutKey).catch(()=>null);if(raced)return json(req,res,200,await resumeExisting(raced,user.id));throw error}if(!saved?.id)throw new Error('The store could not create your order. Please try again.');try{await reserveCheckout(saved.id,user.id)}catch(error){await rest('orders?id=eq.'+encodeURIComponent(saved.id),{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:'payment_failed'})}).catch(()=>null);throw error}if(paymentMethod==='cod'){await rpc('finalize_checkout_order',{p_order_id:saved.id,p_user_id:user.id,p_payment_id:null,p_payment_signature:null,p_target_status:'cod_pending',p_source:'cod'});await rpc('commit_order_inventory',{p_order_id:saved.id});return json(req,res,200,{store_order_id:saved.id,display_order_id:saved.display_order_id,payment_method:'cod',status:'cod_pending',completed:true,cod_fee_non_refundable:false,items:calc.items,pricing:pricingPayload(calc,promo,payment),coupon_code:promo.coupon?.code||null,gift_card_code:promo.gift?.code||null,total:payment.total})}if(payment.total===0){await rpc('finalize_checkout_order',{p_order_id:saved.id,p_user_id:user.id,p_payment_id:null,p_payment_signature:null,p_target_status:'paid',p_source:'gift_card'});await rpc('commit_order_inventory',{p_order_id:saved.id});return json(req,res,200,{store_order_id:saved.id,display_order_id:saved.display_order_id,payment_method:'prepaid',status:'paid',completed:true,zero_value:true,items:calc.items,pricing:pricingPayload(calc,promo,payment),total:0})}let razorpayOrder;try{razorpayOrder=await createRazorpayOrder(saved,user.id)}catch(error){await failCheckout(saved.id,user.id);return json(req,res,502,{error:error.message||'Razorpay order creation failed',retry_with_new_checkout:true})}await rest('orders?id=eq.'+encodeURIComponent(saved.id)+'&user_id=eq.'+encodeURIComponent(user.id),{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({razorpay_order_id:razorpayOrder.id,status:'created'})});return json(req,res,200,{key_id:KEY_ID,store_order_id:saved.id,display_order_id:saved.display_order_id,payment_method:'prepaid',status:'created',order:{id:razorpayOrder.id,amount:razorpayOrder.amount,currency:razorpayOrder.currency},items:calc.items,pricing:pricingPayload(calc,promo,payment),coupon_code:promo.coupon?.code||null,gift_card_code:promo.gift?.code||null,total:payment.total})}catch(error){console.error('create-order error:',error);return json(req,res,400,{error:error.message||'Unable to create order'})}};
