(function(){
'use strict';
let product=null,variants=[],availability=new Map(),selected=null;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function style(){if(document.getElementById('productVariantStyles'))return;const s=document.createElement('link');s.rel='stylesheet';s.href='runtime-product-variants-1.css?v=2';s.id='productVariantStyles';document.head.appendChild(s)}
function label(v){return [v.size,v.color,v.title].filter(Boolean).join(' · ')||v.sku||('Option '+v.id)}
function syncActions(){if(!product)return;const ready=Boolean(selected&&availability.get(String(selected.id))===true);for(const id of ['add','buy']){const button=document.getElementById(id);if(!button)continue;button.disabled=!ready;button.setAttribute('aria-disabled',String(!ready));button.title=ready?'':'Select an available product option first'}}
function updateAvailability(){const box=document.getElementById('availability');if(!box)return;if(!variants.length){box.textContent='No purchasable option is currently available for this product.';syncActions();return}if(!selected){box.textContent='Choose an available option before adding this product to cart.';syncActions();return}box.textContent=availability.get(String(selected.id))===true?'Selected option is currently in stock. Availability is rechecked securely at checkout.':'Selected option is currently unavailable.';syncActions()}
function select(v){if(availability.get(String(v.id))!==true)return;selected=v;window.FashionVariantCart?.set(product.id,v.id);document.querySelectorAll('.variant-option').forEach(b=>{const active=String(b.dataset.variant)===String(v.id);b.classList.toggle('selected',active);b.setAttribute('aria-pressed',String(active))});const price=Number(v.price_override??product.price);const buy=document.getElementById('buyPrice');if(buy)buy.textContent='₹'+price.toLocaleString('en-IN',{maximumFractionDigits:2});const gallery=document.getElementById('gallery');if(v.image_url&&gallery){const img=document.createElement('img');img.src=v.image_url;img.alt=product.name||'Product';img.addEventListener('error',()=>{const fallback=document.createElement('div');fallback.className='fallback';fallback.textContent='Image unavailable';img.replaceWith(fallback)},{once:true});gallery.replaceChildren(img)}updateAvailability()}
function render(){style();const details=document.querySelector('.details');if(!details)return;if(!variants.length){selected=null;window.FashionVariantCart?.remove(product.id);updateAvailability();return}let host=document.getElementById('variantPicker');if(!host){host=document.createElement('div');host.id='variantPicker';host.className='variant-picker';const desc=document.getElementById('description');(desc||details.lastElementChild)?.after(host)}host.innerHTML='<div class="variant-label">SELECT OPTION</div><div class="variant-options">'+variants.map(v=>'<button type="button" class="variant-option" data-variant="'+v.id+'" aria-pressed="false" '+(availability.get(String(v.id))===true?'':'disabled')+'>'+esc(label(v))+'</button>').join('')+'</div><div class="variant-help">Unavailable options cannot be selected. You can add different sizes or colours of this product as separate cart lines.</div>';host.querySelectorAll('.variant-option').forEach(b=>b.onclick=()=>{const v=variants.find(x=>String(x.id)===b.dataset.variant);if(v)select(v)});const saved=window.FashionVariantCart?.get(product.id),candidate=variants.find(v=>v.id===saved&&availability.get(String(v.id))===true),sellable=variants.filter(v=>availability.get(String(v.id))===true);if(candidate)select(candidate);else if(sellable.length===1)select(sellable[0]);else{window.FashionVariantCart?.remove(product.id);selected=null;updateAvailability()}}
async function load(){const id=Number(new URLSearchParams(location.search).get('id'));if(!Number.isInteger(id)||id<=0)return;const{data:p,error:pe}=await supabaseClient.from('products').select('id,name,price,has_variants').eq('id',id).eq('is_active',true).maybeSingle();if(pe)throw pe;if(!p?.has_variants)return;product=p;syncActions();const[{data:vs,error:ve},{data:av,error:ae}]=await Promise.all([supabaseClient.from('product_variants').select('id,product_id,sku,title,size,color,price_override,image_url,is_active').eq('product_id',id).eq('is_active',true).order('id'),supabaseClient.rpc('get_variant_availability',{p_product_id:id})]);if(ve)throw ve;if(ae)throw ae;variants=vs||[];(av||[]).forEach(x=>availability.set(String(x.variant_id),x.in_stock===true));render()}
let tries=0,t=setInterval(()=>{tries++;if(document.getElementById('product')&&!document.getElementById('product').hidden&&window.FashionVariantCart){clearInterval(t);load().catch(error=>{console.error('Product variant selection failed',error);const box=document.getElementById('availability');if(box)box.textContent='Product options could not be loaded. Please refresh and try again.';syncActions()})}else if(tries>80)clearInterval(t)},100);
})();
