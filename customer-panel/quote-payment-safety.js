(function(){'use strict';var page=(location.pathname.split('/').pop()||'').replace(/\.html$/i,'');if(page!=='quote-checkout')return;function normalize(){var el=document.getElementById('status');if(!el)return;var text=String(el.textContent||'');if(text.includes('Reconciling automatically'))el.textContent='Payment captured. Checking your order status; please do not pay again.'}var observer=new MutationObserver(normalize);function start(){var el=document.getElementById('status');if(!el)return;observer.observe(el,{childList:true,subtree:true,characterData:true});normalize()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start()})();