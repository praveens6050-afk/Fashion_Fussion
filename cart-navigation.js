(function(){function wire(){document.querySelectorAll('a,button,[role="button"]').forEach(el=>{const text=(el.textContent||'').trim().toLowerCase(),id=(el.id||'').toLowerCase(),cls=String(el.className||'').toLowerCase();if(text==='cart'||text.startsWith('cart ')||id.includes('cart')&&/(btn|button|icon|link)/.test(id)||cls.includes('cart')&&/(btn|button|icon|link)/.test(cls)){if(el.dataset.ffCartNav==='1')return;el.dataset.ffCartNav='1';el.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();location.href='cart.html'},true)}})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire);else wire();new MutationObserver(wire).observe(document.documentElement,{childList:true,subtree:true})})();