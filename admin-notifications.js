(function(){'use strict';if(!window.supabaseClient)return;async function init(){if((location.pathname.split('/').pop()||'')!=='admin.html')return;const{data:{user}}=await window.supabaseClient.auth.getUser();if(!user)return;const{data:p}=await window.supabaseClient.from('profiles').select('is_admin').eq('id',user.id).maybeSingle();if(!p?.is_admin)return;const host=document.querySelector('.header-actions');if(!host||document.getElementById('ffAdminNotifications'))return;const a=document.createElement('a');a.href='#';a.id='ffAdminNotifications';a.textContent='🔔 Notifications';a.style.position='relative';host.insertBefore(a,host.firstChild);const panel=document.createElement('div');panel.style.cssText='display:none;position:fixed;right:20px;top:75px;width:360px;max-width:calc(100vw - 40px);max-height:65vh;overflow:auto;background:#fff;color:#222;border:1px solid #ddd;border-radius:8px;box-shadow:0 8px 30px #0003;z-index:10000;padding:14px';document.body.appendChild(panel);async function load(){const{data,error}=await window.supabaseClient.from('customer_support_requests').select('id,customer_name,customer_phone,issue,status,created_at').order('created_at',{ascending:false}).limit(30);panel.textContent='';if(error){panel.textContent='Unable to load notifications.';return}const rows=data||[],pending=rows.filter(x=>x.status==='pending');a.textContent='🔔 Notifications'+(pending.length?' ('+pending.length+')':'');const h=document.createElement('h3');h.textContent='Customer Requests';h.style.marginTop='0';panel.appendChild(h);if(!rows.length){const p=document.createElement('p');p.textContent='No customer requests.';panel.appendChild(p);return}rows.forEach(r=>{const item=document.createElement('div');item.style.cssText='border-top:1px solid #eee;padding:10px 0';const name=document.createElement('b');name.textContent=r.customer_name||'Customer';const issue=document.createElement('small');issue.textContent=r.issue||'';const status=document.createElement('small');status.textContent='Status: '+String(r.status||'pending');item.append(name,document.createElement('br'),issue,document.createElement('br'),status);panel.appendChild(item)})}a.onclick=e=>{e.preventDefault();panel.style.display=panel.style.display==='none'?'block':'none';if(panel.style.display==='block')load()};await load();window.supabaseClient.channel('admin-notifications').on('postgres_changes',{event:'*',schema:'public',table:'customer_support_requests'},load).subscribe()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();