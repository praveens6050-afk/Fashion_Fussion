# Launch Readiness Batch 6

This batch keeps internal search-result URLs out of the public sitemap and sends `X-Robots-Tag: noindex, follow` for `/search.html` so crawlers can still follow product links without indexing thin/parameterized search-result pages.

No pricing, checkout, payment, order, inventory, promotion, API, or database behavior is changed.
