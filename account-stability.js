(function(){
'use strict';
if((location.pathname.split('/').pop()||'index.html')!=='account.html')return;
const $=id=>document.getElementById(id);
const titles={orders:['My Orders','Track purchases, delivery progress, cancellations and refunds.'],profile:['Profile','Manage your personal account information.'],addresses:['Addresses','Manage saved delivery addresses and your default checkout address.'],refunds:['Payments & Refunds','Review refund progress linked to your orders.']};
function withTimeout(promise,ms){return Promise.race([promise,new Promise((_,reject)=>setTimeout(()=>reject(new Error('Account request timed out')),ms))])}
function fallbackName(user){const meta=user?.user_metadata||{};const named=String(meta.full_name||meta.name||'').trim();if(named)return named;const mail=String(user?.email||'').split('@')[0].replace(/[._-]+/g,' ').trim();return mail?mail.replace(/\b\w/g,c=>c.toUpperCase()):'Customer'}
function setIdentity(user,profile){const name=String(profile?.full_name||'').trim()||fallbackName(user);const full=$('fullName'),phone=$('phone'),email=$('email'),mini=$('miniName'),avatar=$('avatar');if(full&&!full.value)full.value=name==='Customer'?'':name;if(phone&&!phone.value&&profile?.phone)phone.value=profile.phone;if(email&&!email.value)email.value=user?.email||'';if(mini)mini.textContent=name;if(avatar)avatar.textContent=(name[0]||'U').toUpperCase()}
function ensureView(){const raw=(location.hash||'#orders').slice(1);const view=titles[raw]?raw:'orders';document.querySelectorAll('.view').forEach(el=>el.classList.toggle('active',el.id===view+'View'));document.querySelectorAll('.menu button[data-view]').forEach(el=>el.classList.toggle('active',el.dataset.view===view));if($('viewTitle'))$('viewTitle').textContent=titles[view][0];if($('viewSub'))$('viewSub').textContent=titles[view][1]}
function reveal(){const loading=$('loading'),app=$('app');if(loading)loading.hidden=true;if(app)app.hidden=false;ensureView()}
async function start(){let user=null;try{const sessionResult=await withTimeout(window.supabaseClient.auth.getSession(),3000);user=sessionResult?.data?.session?.user||null;if(!user){const userResult=await withTimeout(window.supabaseClient.auth.getUser(),3000);user=userResult?.data?.user||null}}catch(e){console.warn('Account session fallback failed',e)}if(!user)return;setIdentity(user,null);reveal();try{const result=await withTimeout(window.supabaseClient.from('profiles').select('id,full_name,phone').eq('id',user.id).maybeSingle(),3500);if(result?.error)throw result.error;setIdentity(user,result?.data||null)}catch(e){console.warn('Account profile fallback active',e);const status=$('profileStatus');if(status&&!status.innerHTML)status.innerHTML='<div class="alert error">Profile details could not be refreshed. Orders and account access remain available.</div>'}finally{reveal()}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
window.addEventListener('hashchange',ensureView);
setTimeout(()=>{const app=$('app'),loading=$('loading');if(app&&app.hidden&&loading&&!loading.hidden)start().catch(console.error)},4000);
})();
